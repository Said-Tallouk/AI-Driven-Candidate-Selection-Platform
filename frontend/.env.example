# Base URL of the deployed FastAPI backend, including the /api prefix.
# Leave unset for local dev (Vite proxies /api to http://localhost:8000, see vite.config.js).
VITE_API_URL=https://your-backend.onrender.com/api
